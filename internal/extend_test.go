package internal

import "testing"

func TestParseExtraData(t *testing.T) {
	// data, err := parseTableExtraData("`xo:name=ExName,type=string` 新增ExName字段")
	// spew.Dump(data)
	// assert.NotNil(t, data)
	// assert.Nil(t, err)
}
